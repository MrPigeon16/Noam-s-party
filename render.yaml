services:
  - type: web
    name: flask-app
    env: python
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT
    plan: free
    envVars:
      - key: FLASK_ENV
        value: production
    autoDeploy: true
    region: oregon
    branch: main
    healthCheckPath: /
    runtime: python3
